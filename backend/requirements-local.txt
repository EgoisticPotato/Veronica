# ── Local development extras (not installed in Docker/Cloud Run) ───────────────
# Install alongside requirements.txt for local dev:
#   pip install -r requirements.txt -r requirements-local.txt

# Local STT (STT_PROVIDER=local) — needs ffmpeg system package
faster-whisper==1.1.0

# Local TTS (TTS_PROVIDER=local) — Windows/macOS/Linux with display server
pyttsx3==2.98

# DOCX→PDF via MS Word on Windows (TTS_PROVIDER=local only)
docx2pdf==0.1.8
