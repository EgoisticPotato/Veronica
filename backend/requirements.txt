# Veronica Backend Dependencies — Python 3.13

# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Settings & HTTP
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.28.1

# Web search grounding
tavily-python==0.5.0

# STT
faster-whisper==1.1.0

# TTS
pyttsx3==2.98
elevenlabs==1.9.0

# ── RAG ───────────────────────────────────────────────────────────────────────
pypdf==4.3.1                 # PDF text extraction
pymupdf==1.24.11             # PDF image extraction (import fitz)
sentence-transformers==3.3.1 # Local embeddings — all-MiniLM-L6-v2 (~90MB)
numpy==2.1.3                 # Cosine similarity + vector ops
qdrant-client==1.12.1        # Qdrant vector DB client
aiohttp==3.11.11             # Async URL scraping
beautifulsoup4==4.12.3       # HTML parsing for URL scraping
langchain-text-splitters==0.3.3  # RecursiveCharacterTextSplitter

# ── File conversion ───────────────────────────────────────────────────────────
# ── File conversion ──────────────────────────────────────────────────────────
pdfplumber==0.11.4           # PDF text + table extraction
python-docx==1.1.2           # DOCX assembly and reading
pypdf==4.3.1                 # PDF page count + text extraction
pdf2image==1.17.0            # Render PDF pages for image extraction (needs poppler-utils)
Pillow==10.4.0               # Image handling
pytesseract==0.3.13          # OCR for scanned pages (needs tesseract-ocr system package)
reportlab==4.2.2             # Last-resort DOCX→PDF fallback
docx2pdf==0.1.8              # DOCX → PDF via Word (Windows/macOS)
# pdf2docx kept for reference but no longer used in main pipeline
# pdf2docx==0.5.8
#
# System tools (Linux — run once):
#   sudo apt-get install -y poppler-utils tesseract-ocr libreoffice

# Utilities
python-dotenv==1.0.1

# Screen capture (Agent vision)
mss==9.0.2
